{
  "name": "phasegrid",
  "private": true,
  "description": "PhaseGrid — run npm run dev from this folder",
  "scripts": {
    "dev": "npm-run-all -p dev:api dev:web",
    "dev:web": "npm run dev --prefix web",
    "dev:api": "cd api && python -m uvicorn main:app --reload --host 127.0.0.1 --port 8000",
    "build": "npm run build --prefix web",
    "install:all": "npm install --prefix web && cd api && pip install -r requirements.txt && pip install -r ../requirements.txt"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
